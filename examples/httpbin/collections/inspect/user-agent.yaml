version: 1
kind: request
id: inspect.user-agent
name: Inspect user agent
description: Echoes the User-Agent header Arbor sends.
method: GET
url: "{{base_url}}/user-agent"

assert:
  - status == 200
  - body.user-agent contains "arbor"
